name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  FLUTTER_VERSION: stable

jobs:
  build-android:
    name: Android APK & AAB
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_VERSION }}
          cache: true
      - run: flutter pub get
      - run: flutter build apk --release
      - run: flutter build appbundle --release
      - uses: actions/upload-artifact@v4
        with:
          name: android
          path: |
            build/app/outputs/flutter-apk/app-release.apk
            build/app/outputs/bundle/release/app-release.aab

  build-web:
    name: Web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_VERSION }}
          cache: true
      - run: flutter pub get
      - run: flutter build web --release
      - name: Archive web build
        run: cd build/web && zip -r ../../crisptuner-web.zip .
      - uses: actions/upload-artifact@v4
        with:
          name: web
          path: crisptuner-web.zip

  build-ios:
    name: iOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_VERSION }}
          cache: true
      - run: flutter pub get
      - run: flutter build ios --no-codesign --release
      - name: Archive iOS build
        run: |
          cd build/ios/iphoneos
          zip -r ../../../crisptuner-ios.zip Runner.app
      - uses: actions/upload-artifact@v4
        with:
          name: ios
          path: crisptuner-ios.zip

  build-macos:
    name: macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_VERSION }}
          cache: true
      - run: flutter pub get
      - run: flutter build macos --release
      - name: Archive macOS build
        run: |
          cd build/macos/Build/Products/Release
          zip -r ../../../../../crisptuner-macos.zip "CrispTuner.app"
      - uses: actions/upload-artifact@v4
        with:
          name: macos
          path: crisptuner-macos.zip

  build-linux:
    name: Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_VERSION }}
          cache: true
      - run: |
          sudo apt-get update -y
          sudo apt-get install -y ninja-build libgtk-3-dev libasound2-dev
      - run: flutter pub get
      - run: flutter build linux --release
      - name: Archive Linux build
        run: |
          cd build/linux/x64/release/bundle
          tar czf ../../../../../crisptuner-linux-x64.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: linux
          path: crisptuner-linux-x64.tar.gz

  build-windows:
    name: Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_VERSION }}
          cache: true
      - run: flutter pub get
      - run: flutter build windows --release
      - name: Archive Windows build
        shell: pwsh
        run: Compress-Archive -Path build\windows\x64\runner\Release\* -DestinationPath crisptuner-windows-x64.zip
      - uses: actions/upload-artifact@v4
        with:
          name: windows
          path: crisptuner-windows-x64.zip

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [build-android, build-web, build-ios, build-macos, build-linux, build-windows]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Display artifacts
        run: ls -R artifacts/
      - name: Rename artifacts for release
        run: |
          mv artifacts/android/flutter-apk/app-release.apk artifacts/crisptuner-android.apk
          mv artifacts/android/bundle/release/app-release.aab artifacts/crisptuner-android.aab
          mv artifacts/web/crisptuner-web.zip artifacts/
          mv artifacts/ios/crisptuner-ios.zip artifacts/
          mv artifacts/macos/crisptuner-macos.zip artifacts/
          mv artifacts/linux/crisptuner-linux-x64.tar.gz artifacts/
          mv artifacts/windows/crisptuner-windows-x64.zip artifacts/
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/crisptuner-android.apk
            artifacts/crisptuner-android.aab
            artifacts/crisptuner-web.zip
            artifacts/crisptuner-ios.zip
            artifacts/crisptuner-macos.zip
            artifacts/crisptuner-linux-x64.tar.gz
            artifacts/crisptuner-windows-x64.zip
